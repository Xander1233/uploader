<!DOCTYPE html>
<html lang="en">
<head>
	<meta property="og:host" content="{{{ fileurl }}}">
	<meta property="og:site_name" content="{{ fileid }} • Uploaded by {{ username }}">
	<meta property="og:title" content="{{ title }}">
	<meta property="og:type" content="website">
	<meta property="og:url" content="{{{ fileurl }}}">
	<meta property="twitter:card" content="summary_large_image">
	<meta property="og:image" content="{{{ fileurl }}}">
    <meta property="theme-color" content="{{ color }}">
	<style>
		body {
			margin: 0;
            background-color: {{ background_color }};
		}
		img {
			image-orientation: from-image;
			text-align: center;
			position: absolute;
			max-width: 75%;
			margin: auto;
			top: 0;
			right: 0;
			bottom: 0;
			left: 0;
		}
	</style>
    <title>{{ fileid }} • Uploaded by {{ username }}</title>
</head>
<body>
	<img src="{{{ plain_fileurl }}}" alt="{{ fileid }} • Uploaded by {{ username }}">
</body>
</html>